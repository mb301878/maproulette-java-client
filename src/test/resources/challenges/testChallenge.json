{
  "description": "DESCRIPTION",
  "blurb": "BLURB",
  "instruction": "INSTRUCTION",
  "difficulty": "NORMAL",
  "defaultPriority": "LOW",
  "highPriorityRule": {
    "condition": "AND",
    "rules": [
      {
        "value": "priority_pd.3",
        "type": "string",
        "operator": "equal"
      }
    ]
  },
  "mediumPriorityRule": {
    "condition": "OR",
    "rules": [
      {
        "value": "priority_pd.2",
        "type": "string",
        "operator": "equal"
      }
    ]
  },
  "lowPriorityRule": {
    "condition": "OR",
    "rules": [
      {
        "condition": "OR",
        "rules": [
          {
            "value": "website. ",
            "type": "string",
            "operator": "is_not_empty"
          }
        ]
      },
      {
        "value": "priority_pd.2",
        "type": "string",
        "operator": "equal"
      }
    ]
  }
}
